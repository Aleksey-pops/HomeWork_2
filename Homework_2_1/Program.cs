// using System;
// 1. Дано число обозначающее день недели. Выяснить является номер дня недели выходным

void Print(string[] days)
{
    string[] day = {"Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"};
    {
        for (int j = 0; j < days.Length; j++);
    }    
        Console.WriteLine($"День недели " + {day});
Print(days);
       // return j;
//             //  if (days[j] = day);
// bool Contains(int[] day,int value)
// {
//     foreach(int element in day)
//         if (element == value)
//             return true;
//         return false;  
// }       
}    


// Console.WriteLine(Contains(day, value) ? "There is element into day (В массиве есть  элемент)" : "There is no element into day (В массиве нет элемента)");

// Определить, присутствует ли какой число в массиве?

// void Print(int[] numbers)
// {
//     for (int i = 0; i < numbers.Length; i++)
//     {
//         Console.Write(numbers[i] + " | ");
//     }
// }
// int[] numbers = new int [20];
//         for (int i = 0; i < numbers.Length; i++)
//         {
//              numbers[i] = new Random().Next(-9, 9);
//         }
// Print(numbers);

// Console.WriteLine($"\nВведите число для поиска от -9 до 9: ");
// int num = int.Parse(Console.ReadLine() ?? "0");

// string res = "НЕТ!";
// for (int i = 0; i < numbers.Length; i++)
// {
//     if (numbers[i] == num)
//         res = "ЕСТЬ!";
// }       
// Console.WriteLine($"Такое числа в массиве  {res}");

